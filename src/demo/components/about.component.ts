import {VComponent} from "../../core";

@VComponent({
    selector: 'app-about',
    styles: [],
    html: 'Vienna is just a small tiny framework to speed up development :)'
})
export class AboutComponent {}
