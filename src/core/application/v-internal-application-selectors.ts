export enum VInternalApplicationSelectors {
    // Main selector for Vienna app renderer
    V_APP_RENDERER = 'v-app-renderer',

    // Wrapper selector for every Vienna component
    V_COMPONENT_WRAPPER = 'v-component'
}